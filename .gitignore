.env
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.pyright/
.DS_Store
# Call artifacts ARE committed: recordings, transcripts, evaluations, and metadata are the
# assessment evidence. Only the read-only duplicate copies under calls/.preserved/ are excluded,
# since they are byte-identical backups of calls already committed here and pinned by MD5 in
# docs/evidence/README.md.
calls/.preserved/
.coverage
htmlcov/
